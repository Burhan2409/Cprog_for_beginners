include<stdio.h>
include<stdlib.h>
int main()
{
    char c;
    printf("Enter an alphabet :");
    scanf("%c",&c);

    if((c >= 'a' && c <= 'z') || (c >= 'A' && c <= 'Z'))

        printf("%c is an alphabet",c);

    else

        printf(" The variable %c is not an alphabet",c);

    return 0 ;
}
